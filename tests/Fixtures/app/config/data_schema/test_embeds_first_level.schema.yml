schema:
    class: Glavweb\DatagridBundle\Tests\Fixtures\Entity\Event
    properties:
        name:

        # Test oneToOne
        eventDetail:
            join: none
            properties:
                body:

        # Test manyToOne
        eventGroup:
            join: none
            properties:
                name:

        # Test oneToMany
        sessions:
            join: none
            properties:
                name:

        # Test manyToMany
        tags:
            join: none
            properties:
                name:
